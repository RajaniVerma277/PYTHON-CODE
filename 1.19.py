#write a program in python to take astring from the user and find the maximum index number of that string
statement = input("enter the string")
s = len(statement)
idx = s-1
print("the maximum index of the string is ",idx)
